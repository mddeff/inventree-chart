apiVersion: apps/v1
kind: StatefulSet
metadata:
  annotations: {}
  labels:
    io.kompose.service: {{ include "inventree.name" . }}-db
  name: {{ include "inventree.name" . }}-db
spec:
  podManagementPolicy: OrderedReady
  serviceName: {{ include "inventree.name" . }}-db
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: {{ include "inventree.name" . }}-db
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f resolved.yaml -v -c -o result
        kompose.version: 1.21.0 (992df58d8)
      labels:
        io.kompose.network/default: "true"
        io.kompose.service: {{ include "inventree.name" . }}-db
    spec:
      containers:
      - image: postgres:13
        imagePullPolicy: ""
        name: {{ include "inventree.name" . }}-db
        ports:
          - containerPort: 5432
        resources: {}
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: {{ include "inventree.name" . }}-db
        envFrom:
        - configMapRef:
            name: {{ include "inventree.name" . }}-postgres-config
        - secretRef:
            name: {{ include "inventree.name" . }}-postgres-secret
      restartPolicy: Always
      serviceAccountName: ""
      volumes:
      - name: {{ include "inventree.name" . }}-db
        persistentVolumeClaim:
          claimName: {{ include "inventree.name" . }}-db
