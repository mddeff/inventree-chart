apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    io.kompose.service: {{ include "inventree.name" . }}-cache
  name: {{ include "inventree.name" . }}-cache
spec:
  ports:
  - name: "6379"
    port: 6379
    targetPort: 6379
  selector:
    io.kompose.service: {{ include "inventree.name" . }}-cache

