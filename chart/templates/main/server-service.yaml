apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    io.kompose.service: {{ include "inventree.name" . }}-server
  name: {{ include "inventree.fullname" . }}-server
spec:
  ports:
  - name: "8000"
    port: 8000
    targetPort: 8000
  selector:
    io.kompose.service: {{ include "inventree.name" . }}-server

